# ... (Previous Steps: Checkout, Setup Node.js, Install dependencies, Generate KDP-ready DOCX)

      - name: Commit and push DOCX if changed
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          # FIX: Replaced the failing 'fromJson' with a generic commit message.
          commit_message: "Auto-generate KDP DOCX from book.json [ci skip]" 
          # You can also use a variable if you extracted the title in a previous step,
          # but a generic message is the safest bet to eliminate the JSON parsing error.
          files: |
            *.docx
            book.json
          commit_user_name: github-actions[bot]
          commit_user_email: 41898282+github-actions[bot]@users.noreply.github.com
